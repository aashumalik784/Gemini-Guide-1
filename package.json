{
  "name": "workspace",
  "private": true,
  "scripts": {
    "build": "cd artifacts/gemini-chat && npm install && npm run build",
    "start": "cd artifacts/gemini-chat && npm run preview"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
