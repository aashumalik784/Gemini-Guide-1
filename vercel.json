{
  "buildCommand": "cd artifacts/gemini-chat && npm install && npm run build",
  "outputDirectory": "artifacts/gemini-chat/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
